# Legacy Kernel Experiments

These files keep older multi-algorithm kernel experiments for reference.
They are not compiled by the active CMake build.

The active library kernels in `src/kernels/cuda` keep one default implementation
per operation.
